# CLI Reference

**Status:** placeholder — no application logic exists yet (Phase 1A is scaffold-only).

This document will cover, once the `amxcli` binary has real subcommands:

- Global flags (`--host`, `--auth-token`, config file location overrides).
- Subcommand reference: search, read, send, mutate (move/flag/trash), and
  `mcp` (server lifecycle: `--http`, `--daemon`).
- `amxcli doctor` — environment/permission diagnostics output format.
- Exit codes and machine-readable (`--json`) output conventions.
- Shell completion setup.
