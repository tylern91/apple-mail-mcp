# Migration Notes

**Status:** placeholder — no releases exist yet (Phase 1A is scaffold-only,
current version `0.1.0` unreleased). This document will hold, starting from
the first breaking change:

- One section per major/breaking version, describing what changed and the
  concrete steps to migrate a config file, CLI invocation, or MCP client
  integration forward.
- Cross-references to the `CHANGELOG.md` entries and PRs that introduced each
  break.

Until a breaking change ships, there is nothing to migrate.
